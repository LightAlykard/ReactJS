import React from 'react';
import ReactDOM from 'react-dom';

import App from './components/App.jsx';

/*const App = (props) => {
    return <main>Some textt</main>;
};*/

ReactDOM.render(
    <App/>,
    document.getElementById('root')
);